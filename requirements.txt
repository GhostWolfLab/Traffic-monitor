# 核心依赖 - 按顺序安装
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.10.0

# 机器学习
scikit-learn>=1.3.0
numpy>=1.24.0

# 深度学习 (可选 - 用于Autoencoder检测方法)
# tensorflow>=2.10.0  # 取消注释以启用Autoencoder
# 注意: Apple Silicon Mac请使用 tensorflow-macos

# 网络捕获 - 可能需要额外配置
# scapy>=2.5.0  # Windows需要Npcap

# 异步支持 (二选一)
# eventlet>=0.33.0  # 或者使用下面的gevent
# gevent>=23.0.0
simple-websocket>=1.0.0
